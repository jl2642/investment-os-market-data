# Strategy Kernel v2 — Post-3F Research Path Decision Record

## 2026-08-26 — Governed Post-3F Research Loopback

**Trigger:** Phase 3F completed at 1/4 mandatory promotion requirements with `CONTINUE_SHADOW_RESEARCH`; current fixed Phase 3B forms are `NOT_PROMOTABLE_IN_CURRENT_FORM`, but terminal economic rejection evidence is absent.

**Decision:** approve `NEW_IDENTITY_EVIDENCE_NATIVE_R2_PLUS_INDEPENDENT_HOLDOUT_EXPANSION`.

**Priority:** model-contract redesign is the primary repair path because Phase 3E showed a multi-input schema mismatch and nine single-component ablations restored zero replay coverage. Historical expansion remains mandatory in parallel because a later Phase 3F promotion attempt independently requires broader or holdout evidence.

**R2 direction:** `EVIDENCE_NATIVE_APPLICABILITY_AWARE_PARETO_R2_DESIGN`. This is a design identity only. Before execution it must receive a governed new model/version identity and a frozen Phase 3B-R2 contract. It may not overwrite the fixed Phase 3B forms.

**Data firewall:** the seven existing checkpoints are development corpus only. Phase 3D realized outcomes may not select R2 fields, mappings or thresholds. Same-seed tuning may not count as independent validation. A holdout-selection/sufficiency contract must be frozen before holdout results are observed and must use disjoint point-in-time checkpoints selected without realized-outcome information.

**Rejected:** history-only expansion as the primary repair; redesign-only as a promotion path; retrospective proxy relabelling; subjective fill; retrospective probability/confidence/cost-score creation; outcome tuning.

**Next:** `PHASE_3B_R2_REVISED_MODEL_CONTRACT`, followed by point-in-time R2 replay, independent holdout coverage, downstream 3D/3E evidence as applicable, and a later 3F re-evaluation.

**Not changed:** macro lifecycle Phase 0→5; Phase 4 remains mandatory and blocked; Phase 5 remains unauthorized. No effective policy/economic mutation, recommendation, order or trade authority is created; `orders=0`, `trade_authority=NONE`.
