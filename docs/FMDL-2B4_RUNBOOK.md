# FMDL-2B-4 Runbook

Normal operation is automated. Manual intervention is required only for a multi-session gap, repeated provider failure, an unresolved repair set above the configured limit, or an independent validation failure. In every such case the prior history and factor Current releases remain authoritative until a new candidate passes all gates.
