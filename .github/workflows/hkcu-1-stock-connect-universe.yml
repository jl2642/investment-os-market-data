name: HKCU-1 Stock Connect Universe

on:
  pull_request:
    paths:
      - 'pipeline/hkcu1_*.py'
      - 'config/hkcu1_*.json'
      - 'schemas/hkcu1_*.json'
      - 'tests/test_hkcu1*.py'
      - '.github/workflows/hkcu-1-stock-connect-universe.yml'
  workflow_dispatch:
    inputs:
      as_of_date:
        description: Point-in-time eligibility date (YYYY-MM-DD)
        required: true
        type: string
      publish_candidate:
        description: Build a release candidate after official source resolution
        required: true
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install -r requirements.txt
      - run: pytest -q tests/test_hkcu1_build_universe.py tests/test_hkcu1_r2.py
      - name: Validate JSON contracts
        run: |
          python -m json.tool config/hkcu1_stock_connect_universe_contract.json >/dev/null
          python -m json.tool config/hkcu1_official_source_registry.json >/dev/null
          python -m json.tool schemas/hkcu1_stock_connect_eligibility.schema.json >/dev/null

  official-fetch-validation:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - run: |
          pip install -r requirements.txt
          pip install playwright
          playwright install --with-deps chromium
      - name: Browser-assisted SSE official workbook fetch
        run: |
          rm -rf work/hkcu1
          mkdir -p work/hkcu1/browser
          python pipeline/hkcu1_fetch_sse_browser.py \
            --output-file work/hkcu1/browser/SSE_SH_SOUTHBOUND_BUY_LIST.xls \
            --evidence-file work/hkcu1/browser/HKCU1_SSE_BROWSER_FETCH_EVIDENCE.json
      - name: First parse with independent SZSE bounded retry
        run: |
          passed=0
          for attempt in 1 2 3 4 5 6; do
            rm -rf work/hkcu1/replay_1
            echo "SZSE replay_1 attempt ${attempt}/6; SSE raw file reused"
            python pipeline/hkcu1_fetch_official_lists.py \
              --registry config/hkcu1_official_source_registry.json \
              --output-dir work/hkcu1/replay_1 \
              --sse-raw-file work/hkcu1/browser/SSE_SH_SOUTHBOUND_BUY_LIST.xls \
              --allow-partial
            if python - <<'PY'
          import json
          d=json.load(open('work/hkcu1/replay_1/HKCU1_FETCH_DECISION.json',encoding='utf-8'))
          ok=(d['status']=='PASS' and d['source_rows'].get('SSE_SH_SOUTHBOUND_BUY_LIST')==651 and d['source_rows'].get('SZSE_SZ_SOUTHBOUND_BUY_LIST')==651)
          raise SystemExit(0 if ok else 1)
          PY
            then
              passed=1
              break
            fi
            if [ "$attempt" -lt 6 ]; then sleep $((attempt * 5)); fi
          done
          test "$passed" -eq 1
      - name: Second deterministic parse with independent SZSE bounded retry
        run: |
          passed=0
          for attempt in 1 2 3 4 5 6; do
            rm -rf work/hkcu1/replay_2
            echo "SZSE replay_2 attempt ${attempt}/6; same SSE raw file reused"
            python pipeline/hkcu1_fetch_official_lists.py \
              --registry config/hkcu1_official_source_registry.json \
              --output-dir work/hkcu1/replay_2 \
              --sse-raw-file work/hkcu1/browser/SSE_SH_SOUTHBOUND_BUY_LIST.xls \
              --allow-partial
            if python - <<'PY'
          import json
          d=json.load(open('work/hkcu1/replay_2/HKCU1_FETCH_DECISION.json',encoding='utf-8'))
          ok=(d['status']=='PASS' and d['source_rows'].get('SSE_SH_SOUTHBOUND_BUY_LIST')==651 and d['source_rows'].get('SZSE_SZ_SOUTHBOUND_BUY_LIST')==651)
          raise SystemExit(0 if ok else 1)
          PY
            then
              passed=1
              break
            fi
            if [ "$attempt" -lt 6 ]; then sleep $((attempt * 5)); fi
          done
          test "$passed" -eq 1
      - name: Enforce counts, raw integrity and deterministic normalized output
        run: |
          python - <<'PY'
          import hashlib
          import json
          from pathlib import Path
          import pandas as pd

          root=Path('work/hkcu1')
          browser=json.loads((root/'browser/HKCU1_SSE_BROWSER_FETCH_EVIDENCE.json').read_text(encoding='utf-8'))
          assert browser['status']=='PASS', browser
          raw=(root/'browser/SSE_SH_SOUTHBOUND_BUY_LIST.xls').read_bytes()
          raw_hash=hashlib.sha256(raw).hexdigest()
          assert raw_hash==browser['sha256'], browser

          frames=[]
          ledgers=[]
          for replay in ('replay_1','replay_2'):
              decision=json.loads((root/replay/'HKCU1_FETCH_DECISION.json').read_text(encoding='utf-8'))
              assert decision['status']=='PASS', {replay:decision}
              rows=decision['source_rows']
              assert rows.get('SSE_SH_SOUTHBOUND_BUY_LIST')==651, {replay:rows}
              assert rows.get('SZSE_SZ_SOUTHBOUND_BUY_LIST')==651, {replay:rows}
              assert decision['acquisition_modes']['SSE_SH_SOUTHBOUND_BUY_LIST']=='BROWSER_SESSION_OFFICIAL_FETCH'
              frames.append(pd.read_csv(root/replay/'HKCU1_OFFICIAL_LIST_ROWS.csv',dtype=str))
              ledgers.append(json.loads((root/replay/'HKCU1_SOURCE_LEDGER.json').read_text(encoding='utf-8')))

          def codes(df, channel):
              return sorted(set(df.loc[df['channel']==channel,'security_code'].dropna().astype(str).str.zfill(5)))
          def digest(values):
              return hashlib.sha256(('\n'.join(values)+'\n').encode()).hexdigest()

          replay_hashes={}
          for idx,df in enumerate(frames,1):
              replay_hashes[f'replay_{idx}']={channel:digest(codes(df,channel)) for channel in ('SH','SZ')}
          assert replay_hashes['replay_1']==replay_hashes['replay_2'], replay_hashes

          for ledger in ledgers:
              sse=next(row for row in ledger if row['source_id']=='SSE_SH_SOUTHBOUND_BUY_LIST')
              assert sse['sha256']==raw_hash, sse

          sh=set(codes(frames[0],'SH')); sz=set(codes(frames[0],'SZ'))
          result={
              'as_of_date':'2026-08-06',
              'status':'PASS',
              'sse_acquisition_mode':'BROWSER_SESSION_OFFICIAL_FETCH',
              'same_sse_raw_reused':True,
              'same_raw_double_parse':True,
              'szse_independent_bounded_retry':True,
              'deterministic_normalized_output':True,
              'raw_sha256':raw_hash,
              'normalized_hashes':replay_hashes,
              'sh_count':len(sh),
              'sz_count':len(sz),
              'intersection_count':len(sh & sz),
              'sh_only_count':len(sh-sz),
              'sz_only_count':len(sz-sh),
              'sh_only_codes':sorted(sh-sz),
              'sz_only_codes':sorted(sz-sh),
              'trade_authority':'NONE'
          }
          (root/'HKCU1_R2B_R1_R1_DETERMINISM_DECISION.json').write_text(json.dumps(result,ensure_ascii=False,indent=2),encoding='utf-8')
          print(json.dumps(result,ensure_ascii=False,indent=2))
          PY
      - name: Render evidence
        if: always()
        run: |
          test ! -f work/hkcu1/browser/HKCU1_SSE_BROWSER_FETCH_EVIDENCE.json || cat work/hkcu1/browser/HKCU1_SSE_BROWSER_FETCH_EVIDENCE.json
          test ! -f work/hkcu1/replay_1/HKCU1_FETCH_DECISION.json || cat work/hkcu1/replay_1/HKCU1_FETCH_DECISION.json
          test ! -f work/hkcu1/replay_1/HKCU1_SOURCE_LEDGER.json || cat work/hkcu1/replay_1/HKCU1_SOURCE_LEDGER.json
          test ! -f work/hkcu1/replay_2/HKCU1_FETCH_DECISION.json || cat work/hkcu1/replay_2/HKCU1_FETCH_DECISION.json
          test ! -f work/hkcu1/replay_2/HKCU1_SOURCE_LEDGER.json || cat work/hkcu1/replay_2/HKCU1_SOURCE_LEDGER.json
          test ! -f work/hkcu1/HKCU1_R2B_R1_R1_DETERMINISM_DECISION.json || cat work/hkcu1/HKCU1_R2B_R1_R1_DETERMINISM_DECISION.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: hkcu1-r2b-r1-r1-determinism-evidence
          path: work/hkcu1/**
          if-no-files-found: error

  endpoint-discovery-evidence:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: |
          pip install playwright
          playwright install --with-deps chromium
      - name: Refresh endpoint evidence
        run: |
          python pipeline/hkcu1_discover_official_endpoints.py --output outputs/hkcu1/discovery/HKCU1_OFFICIAL_ENDPOINT_DISCOVERY.json || true
          python pipeline/hkcu1_probe_endpoint_contracts.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: hkcu1-endpoint-evidence
          path: outputs/hkcu1/discovery/*.json
          if-no-files-found: error

  controlled-production:
    if: github.event_name == 'workflow_dispatch' && inputs.publish_candidate
    needs: [test, official-fetch-validation]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stop before publication without sell-only and HKEX cross-check closure
        run: |
          echo 'Buy-list acquisition passed only after R2B-R1-R1. Sell-only ingestion and HKEX cross-check remain mandatory.'
          exit 2
