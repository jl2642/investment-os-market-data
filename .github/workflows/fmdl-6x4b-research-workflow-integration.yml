name: FMDL 6X4-B Research Workflow Integration and Evidence Registration

on:
  pull_request:
    paths:
      - "config/fmdl6x4b_*.json"
      - "schemas/fmdl6x4b_*.json"
      - "scripts/fmdl6x4b_*.py"
      - "tests/test_fmdl6x4b_*.py"
      - "docs/FMDL-6X4-B*.md"
      - ".github/workflows/fmdl-6x4b-research-workflow-integration.yml"
  push:
    branches: [main]
    paths:
      - "config/fmdl6x4b_*.json"
      - "schemas/fmdl6x4b_*.json"
      - "scripts/fmdl6x4b_*.py"
      - "tests/test_fmdl6x4b_*.py"
      - "docs/FMDL-6X4-B*.md"
      - ".github/workflows/fmdl-6x4b-research-workflow-integration.yml"
  workflow_run:
    workflows: ["FMDL 6X4-A Public Equity Investing Adapter and Contract Mapping"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: fmdl-6x4b-${{ github.workflow }}-${{ github.ref || github.event.workflow_run.head_branch }}
  cancel-in-progress: true

jobs:
  quality-gates:
    if: github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_run' && 'main' || github.ref }}
          show-progress: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Contract and integration regression tests
        run: PYTHONPATH=scripts python -m unittest -v tests/test_fmdl6x4b_research_workflow_integration.py
      - name: Compile producer and tests
        run: python -m py_compile scripts/fmdl6x4b_research_workflow_integration.py tests/test_fmdl6x4b_research_workflow_integration.py
      - name: Validate frozen production contract
        run: PYTHONPATH=scripts python scripts/fmdl6x4b_research_workflow_integration.py --repo-root . validate-contract

  production:
    needs: quality-gates
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_run' && 'main' || github.ref }}
          show-progress: false
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Freeze run timestamp and source commit
        run: |
          echo "RUN_AS_OF=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_ENV"
          echo "SOURCE_COMMIT=$(git rev-parse HEAD)" >> "$GITHUB_ENV"
      - name: Build integration candidate
        run: |
          rm -rf outputs/fmdl6x4b/candidate outputs/fmdl6x4b/candidate_replay outputs/fmdl6x4b/acceptance
          PYTHONPATH=scripts python scripts/fmdl6x4b_research_workflow_integration.py --repo-root . build --candidate outputs/fmdl6x4b/candidate --accepted-at "$RUN_AS_OF" --source-commit "$SOURCE_COMMIT"
      - name: Independent same-input replay
        run: PYTHONPATH=scripts python scripts/fmdl6x4b_research_workflow_integration.py --repo-root . validate-candidate --candidate outputs/fmdl6x4b/candidate --accepted-at "$RUN_AS_OF" --source-commit "$SOURCE_COMMIT" --acceptance outputs/fmdl6x4b/acceptance/FMDL6X4B_INDEPENDENT_ACCEPTANCE.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: fmdl6x4b-research-workflow-integration-candidate
          path: |
            outputs/fmdl6x4b/candidate
            outputs/fmdl6x4b/acceptance
          if-no-files-found: error
          retention-days: 30
      - name: Publish Current immutable Release normalized and pointers
        if: (github.event_name == 'push' && github.ref == 'refs/heads/main' && success()) || (github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main' && success())
        run: PYTHONPATH=scripts python scripts/fmdl6x4b_research_workflow_integration.py --repo-root . publish --candidate outputs/fmdl6x4b/candidate --published-at "$RUN_AS_OF" --source-commit "$SOURCE_COMMIT"
      - name: Commit accepted FMDL-6X4-B state
        if: (github.event_name == 'push' && github.ref == 'refs/heads/main' && success()) || (github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main' && success())
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A outputs/fmdl6x4/current/research_workflow_integration datasets/fmdl6x4/releases datasets/fmdl6x4/normalized/research_workflow_integration outputs/status/FMDL6X4B_LAST_SUCCESS.json outputs/status/FMDL6X4_RESEARCH_WORKFLOW_INTEGRATION_LKG.json
          if [ -d outputs/fmdl6x4/archive ]; then git add -A outputs/fmdl6x4/archive; fi
          git diff --cached --quiet && exit 0
          git commit -m "data: publish FMDL-6X4-B workflow integration baseline [skip ci]"
          git push
