name: FMDL 6X3-B Financial Normalization

on:
  pull_request:
    paths:
      - "config/fmdl6x3b_*.json"
      - "schemas/fmdl6x3b_*.json"
      - "scripts/fmdl6x3b_*.py"
      - "tests/test_fmdl6x3b_*.py"
      - "docs/FMDL-6X3-B*.md"
      - ".github/workflows/fmdl-6x3b-financial-normalization.yml"
  push:
    branches: [main]
    paths:
      - "config/fmdl6x3b_*.json"
      - "schemas/fmdl6x3b_*.json"
      - "scripts/fmdl6x3b_*.py"
      - "tests/test_fmdl6x3b_*.py"
      - "docs/FMDL-6X3-B*.md"
      - ".github/workflows/fmdl-6x3b-financial-normalization.yml"
  workflow_run:
    workflows: ["FMDL 6X3-A Research Universe and Data Readiness"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: fmdl-6x3b-${{ github.workflow }}-${{ github.ref || github.event.workflow_run.head_branch }}
  cancel-in-progress: true

jobs:
  quality-gates:
    if: github.event_name != 'workflow_run' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'main')
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_run' && 'main' || github.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Contract and normalization regression tests
        run: PYTHONPATH=scripts python -m unittest -v tests/test_fmdl6x3b_financial_normalization.py
      - name: Compile producer and tests
        run: python -m py_compile scripts/fmdl6x3b_financial_normalization.py tests/test_fmdl6x3b_financial_normalization.py
      - name: Validate frozen production contract
        run: PYTHONPATH=scripts python scripts/fmdl6x3b_financial_normalization.py --repo-root . validate-contract

  production:
    needs: quality-gates
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_run' && 'main' || github.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Freeze run timestamp and source commit
        run: |
          echo "RUN_AS_OF=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_ENV"
          echo "SOURCE_COMMIT=$(git rev-parse HEAD)" >> "$GITHUB_ENV"
      - name: Build financial normalization candidate
        run: |
          rm -rf outputs/fmdl6x3b/candidate outputs/fmdl6x3b/candidate_replay outputs/fmdl6x3b/acceptance
          PYTHONPATH=scripts python scripts/fmdl6x3b_financial_normalization.py --repo-root . build --candidate outputs/fmdl6x3b/candidate --accepted-at "$RUN_AS_OF" --source-commit "$SOURCE_COMMIT"
      - name: Independent captured-input replay
        run: PYTHONPATH=scripts python scripts/fmdl6x3b_financial_normalization.py --repo-root . validate-candidate --candidate outputs/fmdl6x3b/candidate --accepted-at "$RUN_AS_OF" --source-commit "$SOURCE_COMMIT" --acceptance outputs/fmdl6x3b/acceptance/FMDL6X3B_INDEPENDENT_ACCEPTANCE.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: fmdl6x3b-financial-normalization-candidate
          path: |
            outputs/fmdl6x3b/candidate
            outputs/fmdl6x3b/acceptance
          if-no-files-found: error
          retention-days: 30
      - name: Publish Current immutable Release normalized and pointers
        if: (github.event_name == 'push' && github.ref == 'refs/heads/main' && success()) || (github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main' && success())
        run: PYTHONPATH=scripts python scripts/fmdl6x3b_financial_normalization.py --repo-root . publish --candidate outputs/fmdl6x3b/candidate --published-at "$RUN_AS_OF" --source-commit "$SOURCE_COMMIT"
      - name: Commit accepted FMDL-6X3-B state
        if: (github.event_name == 'push' && github.ref == 'refs/heads/main' && success()) || (github.event_name == 'workflow_run' && github.event.workflow_run.head_branch == 'main' && success())
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A outputs/fmdl6x3/current/financial_normalization datasets/fmdl6x3/releases datasets/fmdl6x3/normalized/financial_normalization outputs/status/FMDL6X3B_LAST_SUCCESS.json outputs/status/FMDL6X3_FINANCIAL_NORMALIZATION_LKG.json
          if [ -d outputs/fmdl6x3/archive ]; then git add -A outputs/fmdl6x3/archive; fi
          git diff --cached --quiet && exit 0
          git commit -m "data: publish FMDL-6X3-B financial normalization [skip ci]"
          git push
