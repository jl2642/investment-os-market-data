name: "RETIRED — OCC-R4 Portfolio Decision Freshness"

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical tombstone
        run: |
          echo "OCC-R4 Portfolio Decision Freshness is retired from automatic production."
          echo "S3 Portfolio + Product Surface is the canonical portfolio/user product producer."
          echo "Historical automation/portfolio_freshness code remains for audit only."
          echo "orders=0"
          echo "trade_authority=NONE"
