name: Canonical User Simulation Writeback 20260916

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "automation/canonical_position_update/apply_20260916_user_simulation_update.py"
      - ".github/workflows/canonical-user-simulation-writeback-20260916.yml"
      - "investment_os_runtime/30_STATE_CURRENT/15_PORTFOLIO_INPUT/USER_TRANSACTION_DELTA_LEDGER_CURRENT.json"
      - "investment_os_runtime/30_STATE_CURRENT/20_SIMULATION/**"
      - "investment_os_runtime/40_EVIDENCE_AND_LINEAGE/POSITION_UPDATE_2026_09_16/**"

permissions:
  contents: write
  pull-requests: read

env:
  TRADE_AUTHORITY: NONE

jobs:
  apply-and-validate:
    if: github.head_ref == 'agent/2026-09-16-user-simulation-trade-writeback'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - name: Capture protected-state hashes
        run: |
          sha256sum investment_os_runtime/30_STATE_CURRENT/10_REAL_ACCOUNT/REAL_ACCOUNT_POSITIONS_CURRENT.json > /tmp/real.before
      - name: Apply idempotent user-confirmed Simulation writeback
        run: |
          python -m py_compile automation/canonical_position_update/apply_20260916_user_simulation_update.py
          python automation/canonical_position_update/apply_20260916_user_simulation_update.py
      - name: Persist generated Current and evidence
        shell: bash
        run: |
          set -Eeuo pipefail
          if git diff --quiet; then
            echo 'Already fully materialized.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            investment_os_runtime/30_STATE_CURRENT/15_PORTFOLIO_INPUT/USER_TRANSACTION_DELTA_LEDGER_CURRENT.json \
            investment_os_runtime/30_STATE_CURRENT/20_SIMULATION/SIMULATION_LEDGER_CURRENT.json \
            investment_os_runtime/30_STATE_CURRENT/20_SIMULATION/SIMULATION_POSITIONS_CURRENT.json \
            investment_os_runtime/40_EVIDENCE_AND_LINEAGE/POSITION_UPDATE_2026_09_16/USER_CONFIRMED_SIMULATION_TRADES_20260916.json
          git commit -m 'Materialize user-confirmed 20260916 Simulation trades'
          git push origin "HEAD:${GITHUB_HEAD_REF}"
      - name: Validate idempotency and protected boundaries
        run: |
          python automation/canonical_position_update/apply_20260916_user_simulation_update.py
          git diff --exit-code
          sha256sum -c /tmp/real.before
          python - <<'PY'
          import json
          from pathlib import Path
          p=Path('investment_os_runtime/30_STATE_CURRENT/20_SIMULATION/SIMULATION_POSITIONS_CURRENT.json')
          s=json.loads(p.read_text(encoding='utf-8'))
          q={r['security_id']:r['quantity'] for r in s['holdings']}
          a={r['security_id']:r['available_quantity'] for r in s['holdings']}
          assert q['601138.SH']==800.0 and a['601138.SH']==600.0
          assert q['601899.SH']==1300.0 and a['601899.SH']==1000.0
          assert s['summary']['execution_cash_balance']==247452.41
          assert s['trade_authority']=='NONE'
          src=json.loads(Path('investment_os_runtime/30_STATE_CURRENT/20_SIMULATION/SIMULATION_LEDGER_CURRENT.json').read_text(encoding='utf-8'))
          assert src['summary']['available_cash']==247452.41
          print({'simulation_writeback':'PASS','t_plus_one_availability':'PASS','real_mutations':0,'candidate_mutations':0,'ai_autonomous_mutations':0,'orders':0,'trade_authority':'NONE'})
          PY
      - name: Enforce exact economic scope
        run: |
          git fetch origin main
          python - <<'PY'
          import subprocess
          changed=set(subprocess.check_output(['git','diff','--name-only','origin/main...HEAD'],text=True).splitlines())
          allowed={
            'automation/canonical_position_update/apply_20260916_user_simulation_update.py',
            '.github/workflows/canonical-user-simulation-writeback-20260916.yml',
            'investment_os_runtime/30_STATE_CURRENT/15_PORTFOLIO_INPUT/USER_TRANSACTION_DELTA_LEDGER_CURRENT.json',
            'investment_os_runtime/30_STATE_CURRENT/20_SIMULATION/SIMULATION_LEDGER_CURRENT.json',
            'investment_os_runtime/30_STATE_CURRENT/20_SIMULATION/SIMULATION_POSITIONS_CURRENT.json',
            'investment_os_runtime/40_EVIDENCE_AND_LINEAGE/POSITION_UPDATE_2026_09_16/USER_CONFIRMED_SIMULATION_TRADES_20260916.json',
          }
          leaked=sorted(changed-allowed)
          if leaked: raise SystemExit('UNEXPECTED_SCOPE:' + ','.join(leaked))
          print({'exact_scope':'PASS','files':len(changed)})
          PY
