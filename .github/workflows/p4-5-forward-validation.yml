name: "RETIRED — P4-5 Clean Baseline Forward Parallel Validation"

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  TRADE_AUTHORITY: NONE

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical tombstone
        run: |
          echo "P4-5 Clean Baseline Forward Parallel Validation is retired from automatic production."
          echo "reason=DEPENDS_ON_RETIRED_P4_3_P4_4_PRODUCT_CHAIN"
          echo "Historical implementation remains in the repository for audit only."
          echo "orders=0"
          echo "trade_authority=NONE"
