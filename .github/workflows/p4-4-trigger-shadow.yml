name: "RETIRED — P4-4 Trigger Monitor and Autonomous Shadow Book"

on:
  workflow_dispatch:

permissions:
  contents: read

env:
  TRADE_AUTHORITY: NONE

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Historical tombstone
        run: |
          echo "P4-4 Trigger Monitor and Autonomous Shadow Book is retired from automatic production."
          echo "reason=S2/S3_REPLACED_OLD_P4_3_RECOMMENDATION_TRIGGER_PATH"
          echo "Historical implementation remains in the repository for audit only."
          echo "orders=0"
          echo "trade_authority=NONE"
